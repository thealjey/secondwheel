language: node_js
cache:
  yarn: true
  directories:
    - node_modules
git:
  quiet: true
  submodules: false
notifications:
  email: false
node_js: lts/*
after_success:
  - yarn travis-deploy-once "yarn semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
